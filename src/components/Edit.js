var React = require('react');

const Edit = (props) => {
    console.log(props);
    return (
        <div>
            This is from edit expense page
        </div>
    );    
};
export default Edit;